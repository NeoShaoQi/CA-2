<% if (currentUser) { %>

    <div class="alert alert-success text-center">
        Welcome,
        <strong><%= currentUser.username %></strong>
    </div>

    <div class="row g-3">

        <!-- Workout -->
        <div class="col-md-6">
            <a href="/workouts" class="btn btn-primary w-orkout -->
        <div class="col-md-6">
            /addWorkoutbtn btn-success w-100 p-3">
                ➕ Add Workout
            </a>
        </div>

        <!-- Calories -->
        <div class="col-md-6">
            " class="btn btn-warning w-100 p-3">
                🍎 View Calories
            </a>
        </div>

        <!-- Add Calories -->
        <div class="col-md-6">
            <a href="/addCalories" class="btn btn-infoBMI -->
        <div class="col-md-12">
            <a href="/bmi" class="btn btn-secondary w-ntUser.role === 'admin') { %>
            <div class="col-md-12">
                /admin w-100 p-3">
                    🛠️ Admin Dashboard
                </a>
            </div>
        <% } %>

        <div class="col-md-12">
            /logout-danger w-100 p-3">
                Log Out
            </a>
        </div>

    </div>

<% } else { %>

    <div class="alert alert-warning">
        <p class="mb-0">
            Please log in or create an account to access the workout and calorie tracking tools.
        </p>
    </div>

    <div class="d-grid gap-3 mt-3 col-md-6 mx-auto">

        " class="btn btn-primary">
            Log In
        </a>

        /register
            Register New Account
        </a>

    </div>

<% } %>